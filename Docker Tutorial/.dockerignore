node_modules
.git
.gitignore
.vscode


<!-- The .dockerignore file tells Docker which files/folders to ignore when building an image.

Any files listed in .dockerignore will not be sent to the Docker daemon during docker build.

This is useful to reduce image size and avoid sending sensitive or unnecessary files. -->